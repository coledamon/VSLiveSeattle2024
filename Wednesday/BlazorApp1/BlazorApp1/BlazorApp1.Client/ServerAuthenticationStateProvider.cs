using System.Security.Claims;

namespace BlazorApp1.Client
{
    public class ServerAuthenticationStateProvider(HttpClient httpClient) : ServerAuthenticationStateProvider
    {
        public async override Task<AuthenticationState>
    }
}
